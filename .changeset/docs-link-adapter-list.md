---
"@payfanout/react": patch
"@payfanout/server": patch
---

Point the package READMEs at the full list of shipped adapters instead of naming two of them, so the install instructions stay accurate as further providers ship.
